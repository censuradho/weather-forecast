interface Colors {
  primary: string,
  primaryLight: string,
  background: string,
  foreground: string,
  color: string,
  heading: string,
  backgroundGradient: string,
}

export interface Theme {
  colors: Colors,
  borderRadius: string,
}